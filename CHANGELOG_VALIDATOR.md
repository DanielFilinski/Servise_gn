# ✅ ОБНОВЛЕНИЕ: Трехэтапный процесс валидации

## 🎯 Новая логика работы

Валидация разделена на **3 независимых этапа** для максимального контроля качества.

---

## 📝 Этапы

### 1️⃣ Анализ и генерация шаблонов

```bash
npm run validate
```

- ✅ Анализирует все ссылки в SS+.json
- ✅ Находит ошибки и пропущенные ссылки
- ✅ Автоматически создает шаблоны исправлений
- ✅ Сохраняет в bibleLinksCorrections.json
- ❌ НЕ изменяет файлы

**Результат**: Файл с шаблонами, где `autoApply: false`

---

### 2️⃣ Ручная проверка и редактирование

Откройте `bibleLinksCorrections.json` и:

1. **Проверьте данные ссылок** - исправьте chapter, verses, bookNumber
2. **Установите autoApply** - `true` для применения, `false` для пропуска

**Пример:**

```json
{
  "from": "1) «перейти» (Нав. 1:1–5:12);",
  "info": { ... },
  "links": [
    {
      "text": "Нав. 1:1–5:12",
      "data": [{
        "bookNumber": 60,
        "chapter": [1, 2, 3, 4, 5],  ← Исправлено
        "verses": null                ← Исправлено
      }]
    }
  ],
  "autoApply": true  ← Установлено вручную
}
```

---

### 3️⃣ Применение исправлений

```bash
npm run apply
```

- ✅ Загружает SS+.json (оригинал)
- ✅ Применяет ТОЛЬКО исправления с `autoApply: true`
- ✅ Создает SS-valid.json с исправлениями
- ✅ Показывает статистику

**Результат**: Файл SS-valid.json с примененными исправлениями

---

## 🔄 Полный цикл работы

```bash
# 1. Парсинг
npm run ss

# 2. Генерация шаблонов
npm run validate

# 3. Редактирование bibleLinksCorrections.json
# (откройте файл и отредактируйте вручную)

# 4. Применение
npm run apply

# 5. Повторная проверка (опционально)
npm run validate
```

---

## 💡 Ключевые изменения

| До | После |
|----|-------|
| Автоматическое применение | Ручная проверка перед применением |
| Один скрипт для всего | Два отдельных скрипта |
| Риск неправильных исправлений | Полный контроль |
| Непрозрачный процесс | Прозрачность на каждом этапе |

---

## 📊 Статистика

После `npm run validate`:
```
✅ Добавлено 36 новых шаблонов в bibleLinksCorrections.json
```

После `npm run apply`:
```
✅ Применено исправлений: 1
⚠️  Осталось проблем: 35
```

---

## 🎓 Документация

- **WORKFLOW.md** - Подробное описание процесса с примерами
- **VALIDATOR_README.md** - Общий обзор системы
- **BIBLE_LINK_VALIDATOR.md** - Техническая документация

---

## ✨ Преимущества

✅ **Безопасность** - оригинал SS+.json не изменяется  
✅ **Контроль** - ручная проверка каждого исправления  
✅ **Гибкость** - можно применять по частям  
✅ **Прозрачность** - видно что и как исправляется  
✅ **Откат** - можно отменить в любой момент

---

**Дата обновления:** 11 ноября 2025  
**Версия:** 3.0 (Three-Step Process)
